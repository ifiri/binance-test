import {
  setProducts,
  setFilter,
  setSearch,
  updateProducts,
  sort,
  loadingStart,
  loadingEnd,
} from './productsSlice';

import {
  fetchProducts,
} from './actions';

export {
  setProducts,
  setFilter,
  setSearch,
  updateProducts,
  sort,
  loadingStart,
  loadingEnd,
  fetchProducts,
};

export { default } from './productsSlice';
