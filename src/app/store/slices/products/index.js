import {
  setProducts,
  setFilter,
  updateProducts,
  loadingStart,
  loadingEnd,
} from './slice';

import {
  fetchProducts,
} from './actions';

export {
  setProducts,
  setFilter,
  updateProducts,
  loadingStart,
  loadingEnd,
  fetchProducts,
};

export { default } from './slice';
