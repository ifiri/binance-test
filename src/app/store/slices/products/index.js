import {
  setProducts,
  setFilter,
  updateProducts,
  loadingStart,
  loadingEnd,
} from './productsSlice';

import {
  fetchProducts,
} from './actions';

export {
  setProducts,
  setFilter,
  updateProducts,
  loadingStart,
  loadingEnd,
  fetchProducts,
};

export { default } from './productsSlice';
