import * as actionCreators from './actionCreators';

export const {
  setProductsCreator,
  updateProductsCreator,
  setFilterCreator,
  setSearchCreator,
  sortCreator,
  loadingStartCreator,
  loadingEndCreator,
} = actionCreators;

export default actionCreators;
