import * as actionCreators from './actionCreators';

export const {
  setProductsCreator,
  updateProductsCreator,
  setFilterCreator,
  loadingStartCreator,
  loadingEndCreator,
} = actionCreators;

export default actionCreators;
