import * as selectors from './selectors';

export const {
  selectFilter,
  selectSearch,
  selectLoadingState,
  selectProductsExistence,
  selectProducts,
  selectSort,
} = selectors;

export default selectors;
