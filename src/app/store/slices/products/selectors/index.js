import * as selectors from './selectors';

export const {
  selectFilter,
  selectSearchResults,
  selectLoadingState,
  selectProductsExistence,
  selectProducts,
} = selectors;

export default selectors;
